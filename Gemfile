source "https://rubygems.org"

# 与 GitHub Pages 线上构建环境保持一致（含 optional-front-matter、
# relative-links、titles-from-headings、default-layout 等插件）。
# 本地预览：bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"
